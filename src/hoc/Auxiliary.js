// This component id created to statisfy the React's rule that each component return only ONE JSX root element.

// Also version 16.2 <, has built in Auxiliary component called React.Fragment or import Fragment from 'react' use Fragment

const Auxiliary = (props) => props.children;

export default Auxiliary;